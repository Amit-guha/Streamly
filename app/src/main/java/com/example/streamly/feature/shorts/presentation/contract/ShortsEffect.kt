package com.example.streamly.feature.shorts.presentation.contract

/** No one-time events yet — back navigation is a direct [onBack] callback (same pattern as the
 * player screen), and like/comment/share are visual-only stubs with no side effects. */
sealed interface ShortsEffect